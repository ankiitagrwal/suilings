[package]
name = "suilings_runner"
version = "0.0.1"
edition = "2024.beta"

[addresses]
suilings = "0x0"

[dependencies]
# Sui dependency is automatically injected by Move 2024 edition
# Removing explicit dependency to allow automatic framework injection